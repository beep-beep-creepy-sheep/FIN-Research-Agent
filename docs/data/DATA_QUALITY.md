# Data Quality

Stage 3 stores idempotent data-quality issues for missing metadata, download/parse failures, OCR requirement, duplicate/conflicting filings, issuer mapping conflicts, unofficial facts pending verification, stale sources, and future-publication leakage risk.

Issue states are `open`, `acknowledged`, `resolved`, and `ignored_with_reason`. Severe conflicts require explicit human or deterministic resolution.
